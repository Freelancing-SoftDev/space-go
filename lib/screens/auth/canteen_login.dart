import 'package:flutter/material.dart';

class CanteenLogin extends StatelessWidget {
  const CanteenLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
